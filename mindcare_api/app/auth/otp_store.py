# DEPRECATED — replaced by app/auth/otp_service.py (MySQL-backed).
# This module is no longer imported anywhere and will be removed in a future cleanup.
raise ImportError(
    "otp_store is deprecated. Use app.auth.otp_service instead."
)
