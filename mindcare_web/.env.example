# Лимит загрузки изображений теперь берётся с бэкенда через GET /api/public/config.
# REACT_APP_NEWS_IMAGE_MAX_SIZE_MB больше не используется.
# Для изменения лимита — правьте NEWS_IMAGE_MAX_SIZE_MB в mindcare_api/.env
